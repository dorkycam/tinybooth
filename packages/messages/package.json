{
  "name": "@tinybooth/messages",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Random message library shown after each photo. Migrated from the original Swift app.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./library": "./src/library.ts"
  },
  "scripts": {
    "lint": "echo \"lint stub\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo \"no build (consumed via TS source)\""
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
