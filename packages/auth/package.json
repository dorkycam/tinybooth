{
  "name": "@tinybooth/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Supabase Auth wrappers (browser + server) plus a debug-header dev fallback used by every TinyBooth app.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./server": "./src/server.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "lint": "echo \"lint stub\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo \"no build (consumed via TS source)\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@tinybooth/config": "workspace:*",
    "@types/node": "^20.11.30",
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
