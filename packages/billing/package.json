{
  "name": "@tinybooth/billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for product catalog, prices, and entitlement evaluation. Consumed by web, mobile, and webhook handlers.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./products": "./src/products.ts",
    "./entitlements": "./src/entitlements.ts"
  },
  "scripts": {
    "lint": "echo \"lint stub\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "echo \"no build (consumed via TS source)\""
  },
  "devDependencies": {
    "@tinybooth/config": "workspace:*",
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
