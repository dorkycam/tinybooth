{
  "name": "@tinybooth/api-types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared TypeScript interfaces for the TinyBooth domain (Event, Post, Photo, Strip, User).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"lint stub\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo \"no build (consumed via TS source)\""
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
