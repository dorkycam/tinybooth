# Fastlane outputs and per-machine state. Anything generated by `fastlane`
# itself is regenerable from the lanes in Fastfile.
report.xml
Preview.html
test_output/
screenshots/
metadata/review_information/

# Local credentials (Apple ID password, API key paths). Never committed.
.env
.env.*
!.env.example
